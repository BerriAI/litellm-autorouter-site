# LiteLLM AutoRouter landing page

Static landing page for the LiteLLM AutoRouter early-access beta. The form POSTs JSON to a webhook (set WEBHOOK_URL in index.html). Deployed on Render as a static site.
